#include <stdio.h>

int main(void)
{
    int N , X;
    scanf("%d %d",&N,&X);
    for(int i = 0; i < X; i++)
    {
        if(A < X)
        {
            printf("%d",&A)
        }
    }
    return 0;
}